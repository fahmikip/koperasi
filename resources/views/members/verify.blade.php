<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Verifikasi Anggota</title>@vite(['resources/css/app.css'])</head><body class="grid min-h-screen place-items-center bg-slate-100 p-4"><main class="w-full max-w-md rounded-3xl bg-white p-8 text-center shadow-xl"><div class="mx-auto h-28 w-24 overflow-hidden rounded-2xl bg-slate-100">@if($member->photo_path)<img src="{{ Storage::url($member->photo_path) }}" class="h-full w-full object-cover">@endif</div><div class="mt-5 text-xs font-bold uppercase tracking-widest text-blue-600">Anggota Terverifikasi</div><h1 class="mt-2 text-2xl font-black">{{ $member->name }}</h1><p class="font-mono text-slate-500">{{ $member->member_number }}</p><div class="mt-6 rounded-2xl {{ $member->status==='active'?'bg-emerald-50 text-emerald-700':'bg-red-50 text-red-700' }} p-4 font-bold">{{ $member->status==='active'?'AKTIF':'TIDAK AKTIF' }}</div><p class="mt-4 text-sm text-slate-500">Berlaku sampai {{ $member->valid_until->translatedFormat('d F Y') }}</p></main></body></html>
