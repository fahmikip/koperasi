<x-app-layout><x-slot name="header"><div class="flex items-center justify-between"><h1 class="text-2xl font-bold">Data Anggota</h1>@can('members.create')<a href="{{ route('members.create') }}" class="rounded-xl bg-blue-700 px-4 py-2 text-sm font-semibold text-white">Tambah Anggota</a>@endcan</div></x-slot>
<div class="rounded-2xl bg-white p-5 shadow-sm"><form class="mb-5 flex flex-wrap gap-3"><input name="search" value="{{ request('search') }}" placeholder="Cari nama, NIK, nomor..." class="rounded-xl border-slate-200"><select name="status" class="rounded-xl border-slate-200"><option value="">Semua status</option><option value="active" @selected(request('status')==='active')>Aktif</option><option value="inactive" @selected(request('status')==='inactive')>Nonaktif</option></select><button class="rounded-xl bg-slate-800 px-4 text-white">Filter</button></form>
<div class="overflow-x-auto"><table class="w-full text-sm"><thead class="text-left text-slate-500"><tr><th class="py-3">Nomor</th><th>Nama</th><th>WhatsApp</th><th>Status</th><th></th></tr></thead><tbody>@forelse($members as $member)<tr class="border-t"><td class="py-4 font-mono">{{ $member->member_number }}</td><td class="font-semibold">{{ $member->name }}</td><td>{{ $member->whatsapp }}</td><td><span class="rounded-full px-3 py-1 text-xs {{ $member->status==='active'?'bg-emerald-100 text-emerald-700':'bg-slate-100 text-slate-600' }}">{{ $member->status==='active'?'Aktif':'Nonaktif' }}</span></td><td class="text-right"><a href="{{ route('members.show',$member) }}" class="font-semibold text-blue-700">Detail</a></td></tr>@empty<tr><td colspan="5" class="py-12 text-center text-slate-400">Data belum tersedia.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $members->links() }}</div></div></x-app-layout>
