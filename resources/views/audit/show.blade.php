<x-app-layout>
    <x-slot name="header"><div class="flex flex-wrap items-end justify-between gap-4"><div><div class="text-xs font-bold uppercase tracking-[.2em] text-gold-600">Audit Log #{{ $activity->id }}</div><h1 class="mt-1 font-display text-3xl font-bold text-blue-950">Detail Aktivitas</h1><p class="mt-1 text-sm text-slate-500">Rekaman tidak dapat diubah dari halaman ini.</p></div><a href="{{ route('audit.index') }}" class="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-bold text-slate-600">← Kembali</a></div></x-slot>
    <div class="grid gap-6 lg:grid-cols-3"><aside class="rounded-2xl bg-gradient-to-br from-blue-950 to-blue-700 p-6 text-white shadow-soft"><div class="text-xs font-bold uppercase tracking-wider text-gold-300">{{ $service->eventLabel($activity) }}</div><div class="mt-3 text-xl font-bold">{{ $activity->description }}</div><dl class="mt-6 space-y-4 text-sm"><div><dt class="text-blue-300">Waktu</dt><dd class="mt-1 font-semibold">{{ $activity->created_at->translatedFormat('d F Y, H:i:s') }}</dd></div><div><dt class="text-blue-300">Pelaku</dt><dd class="mt-1 font-semibold">{{ $activity->causer?->name ?? 'Sistem' }}</dd><dd class="text-xs text-blue-200">{{ $activity->causer?->email }}</dd></div><div><dt class="text-blue-300">Modul / Subjek</dt><dd class="mt-1 font-semibold">{{ $service->subjectLabel($activity->subject_type) }} · {{ $service->subjectIdentity($activity) }}</dd></div></dl></aside><section class="rounded-2xl border border-slate-100 bg-white p-6 shadow-soft lg:col-span-2"><h2 class="font-display text-lg font-bold text-blue-950">Perubahan Data</h2><p class="mt-1 text-xs text-slate-400">Perbandingan nilai sebelum dan sesudah aktivitas.</p>@if(count($changes))<div class="mt-5 overflow-x-auto"><table class="w-full text-sm"><thead><tr><th class="py-3 text-left">Field</th><th class="text-left">Sebelum</th><th class="text-left">Sesudah</th></tr></thead><tbody>@foreach($changes as $change)<tr class="border-t border-slate-100"><td class="py-3 font-bold text-slate-700">{{ $service->fieldLabel($change['field']) }}</td><td class="max-w-xs whitespace-pre-wrap break-words text-rose-700">{{ $service->displayValue($change['old']) }}</td><td class="max-w-xs whitespace-pre-wrap break-words text-emerald-700">{{ $service->displayValue($change['new']) }}</td></tr>@endforeach</tbody></table></div>@else<div class="mt-5 rounded-xl bg-slate-50 p-5 text-sm text-slate-500">Aktivitas ini tidak memiliki perubahan field terstruktur.</div>@endif@if($activity->properties->isNotEmpty())<details class="mt-6 rounded-xl border border-slate-200 p-4"><summary class="cursor-pointer text-sm font-bold text-slate-600">Metadata teknis</summary><pre class="mt-3 overflow-x-auto whitespace-pre-wrap break-words rounded-lg bg-slate-950 p-4 text-xs text-slate-200">{{ json_encode($activity->properties, JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE) }}</pre></details>@endif</section></div>
</x-app-layout>
